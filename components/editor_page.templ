package components

import "github.com/brocode/neoweb/nvimwrapper"

css editorWrapper() {
	max-width: fit-content;
	margin-left: auto;
	margin-right: auto;
}

templ EditorPage(nvimResult nvimwrapper.NvimResult) {
	@BasePage() {
		<script type="text/javascript" src="/static/editor.js"></script>
		<div id="editor" class={ editorWrapper() }>
			@Editor(nvimResult)
		</div>
		<script>
            initKeyListener()
            initEventSource()
            initPaste()
        </script>
	}
}
